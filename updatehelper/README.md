place all required language files in lang/
you will need all the .lang files from Desktop edition, with the `en_US.lang` file renamed `en_US.lang.desktop`.
Then, copy the Pocket Edition lang file into lang/
